{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the splash and main windows.",
  "windows": ["splash", "main"],
  "permissions": ["core:default", "core:event:default", "core:window:default"]
}
